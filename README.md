# sms-spam-classifier
A SMS Spam Classifier is a machine learning application that automatically identifies whether a text message is spam (unwanted/promotional/fraudulent) or ham (normal/legitimate message). It analyzes the content of SMS messages and predicts their category based on patterns learned from previously labeled data.
